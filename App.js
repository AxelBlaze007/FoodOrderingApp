import React from "react";
import ReactDOM from "react-dom/client";
// *
const Components = () => <h1>demo</h1>;
const title = (
        <h1>Helo</h1>
);

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(title);
